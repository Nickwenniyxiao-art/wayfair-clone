steps:
  # Step 1: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/wayfair-clone:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/wayfair-clone:latest'
      - '.'

  # Step 2: Push image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/wayfair-clone:$SHORT_SHA'

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'wayfair-clone'
      - '--image=gcr.io/$PROJECT_ID/wayfair-clone:$SHORT_SHA'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--memory=2Gi'
      - '--cpu=2'
      - '--timeout=3600'
      - '--set-env-vars=DATABASE_URL=$_DATABASE_URL,JWT_SECRET=$_JWT_SECRET,VITE_APP_ID=$_VITE_APP_ID,OAUTH_SERVER_URL=$_OAUTH_SERVER_URL,VITE_OAUTH_PORTAL_URL=$_VITE_OAUTH_PORTAL_URL,BUILT_IN_FORGE_API_URL=$_BUILT_IN_FORGE_API_URL,BUILT_IN_FORGE_API_KEY=$_BUILT_IN_FORGE_API_KEY'

images:
  - 'gcr.io/$PROJECT_ID/wayfair-clone:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/wayfair-clone:latest'

substitutions:
  _DATABASE_URL: ''
  _JWT_SECRET: ''
  _VITE_APP_ID: ''
  _OAUTH_SERVER_URL: ''
  _VITE_OAUTH_PORTAL_URL: ''
  _BUILT_IN_FORGE_API_URL: ''
  _BUILT_IN_FORGE_API_KEY: ''

options:
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: '3600s'
